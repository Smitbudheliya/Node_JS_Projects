<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show Blog</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: #f9f9f9;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .navbar {
      background: #fff;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      padding: 12px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .navbar-brand {
      font-weight: bold;
      font-size: 20px;
      color: #333;
      text-decoration: none;
    }

    .navbar .ms-auto {
      display: flex;
      gap: 10px;
    }

    .btn-yellow {
      background: #ffb400;
      color: #fff;
      padding: 8px 14px;
      border-radius: 6px;
      font-weight: bold;
      text-decoration: none;
      border: none;
      cursor: pointer;
      display: inline-block;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }

    .my-4 {
      margin: 20px 0;
    }

    .d-flex {
      display: flex;
      gap: 10px;
    }

    .form-control {
      flex: 1;
      padding: 8px 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
    }

    .btn-warning {
      background: #ffc107;
      border: none;
      padding: 8px 14px;
      border-radius: 6px;
      font-weight: bold;
      cursor: pointer;
    }

    .row {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
    }

    .blog-card {
      background: #fff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
    }

    .blog-card:hover {
      transform: translateY(-5px);
    }

    .blog-card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      display: block;
    }

    .card-body {
      padding: 15px;
    }

    .category-badge {
      background: #ffe7d4;
      color: #ff7b00;
      font-size: 12px;
      padding: 4px 10px;
      border-radius: 20px;
      display: inline-block;
    }

    h5 {
      margin: 10px 0 5px;
      font-size: 18px;
      color: #333;
    }

    .text-muted {
      color: #666;
      font-size: 14px;
      margin-bottom: 10px;
    }

    .btn-read {
      background: linear-gradient(90deg, #ff7b00, #ffb347);
      border: none;
      color: white;
      font-weight: bold;
      border-radius: 20px;
      padding: 6px 14px;
      text-decoration: none;
      display: inline-block;
    }
  </style>
</head>

<body>

  <nav class="navbar">
    <a class="navbar-brand" href="/">MyBlog</a>
    <div class="ms-auto">
      <a href="/" class="btn-yellow">Add Blog</a>
      <a href="/show" class="btn-yellow">Show Blogs</a>
    </div>
  </nav>

  <div class="container my-4">
    <div style="display: flex; justify-content: center;">
      <form class="d-flex" style="max-width: 500px; width: 100%;">
        <input class="form-control" type="search" placeholder="Search blogs...">
        <button class="btn-warning">Search</button>
      </form>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <% blog.forEach((val)=> { %>
        <div class="col-md-4">
          <div class="blog-card">
            <img src="/uploads/<%= val.profile %>" alt="<%= val.title %>">
            <div class="card-body">
              <span class="category-badge">
                <%= val.category %>
              </span>
              <h5>
                <%= val.title %>
              </h5>
              <p class="text-muted">
                <%= val.description %>
              </p>
              <a href="#" class="btn-read">Read more</a>
            </div>
          </div>
        </div>
        <% }) %>
    </div>
  </div>
</body>

</html>